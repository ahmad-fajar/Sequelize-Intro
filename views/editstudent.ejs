<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Student</title>
  </head>
  <body>

    <h2>Edit <%= data[0].first_name %> <%= data[0].last_name %>'s data.</h2>
    <br>

    <form class="" action="/students/edit/<%= data[0].id %>" method="post">
      <input type="text" name="first_name" value="<%= data[0].first_name %>">
      <br>
      <input type="text" name="last_name" value="<%= data[0].last_name %>">
      <br>
      <input type="text" name="email" value="<%= data[0].email %>">
      <br>
      <button type="submit">Submit</button>

    </form>
  </body>
</html>