<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Subjects</title>
  </head>
  <body>
    <h1>Subjects</h1>

    <table>
      <tr>
        <th>ID</th>
        <th>Subject</th>
      </tr>

    <% data.forEach(list => { %>
      <tr>
        <td><%= list.id %></td>
        <td><%= list.subject_name %></td>
      </tr>
    <% }); %>

    </table>

  </body>
</html>